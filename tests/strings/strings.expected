const empty = String literal: ""
;
const simple = String literal: "hello world"
;
const escapes = String literal: "nl:\n tab:\t cr:\r quote:\" apos:\' backslash:\\ hex:\x41 uni:\u{1F4A9}"
;
const ends_with_escaped_quote = String literal: "trailing \""
;
const escaped_backslash = String literal: "ends with a backslash \\"
;
const slashes = String literal: "// not a comment"
;
const doc_slashes = String literal: "/// not a doc comment"
;
const ml_marker = String literal: "a \\ backslash pair inside a string"
;
const two = String literal: "first"
 ++ String literal: "second"
;

const indented =
    Multiline string literal: first line
second line
;

const unindented =
Multiline string literal: no indent here
still going
;

const with_blank =
    Multiline string literal: before

after
;

const ml_contents =
    Multiline string literal: has "quotes" and // slashes and \n literal backslash-n
;

const after_ml = String literal: "back to normal"
;
